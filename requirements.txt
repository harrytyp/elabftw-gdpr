# eLabFTW GDPR disclosure tooling - Python dependencies
# Installed automatically into .venv on first run (see gdpr.py).
elabapy>=1.1.0     # official eLabFTW API wrapper (pulls in requests)
pillow>=10.0       # thumbnails for the HTML explorer
reportlab>=4.0     # PDF disclosure letter
colorama>=0.4.6    # reliable ANSI color support on Windows CMD/PowerShell
# argcomplete>=3.0  # optional: tab completion (Linux/macOS), see README
# Install argcomplete separately if tab completion is needed.
